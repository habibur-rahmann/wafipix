import { FC } from 'react'

interface BreadCrumbProps {
  
}

const BreadCrumb: FC<BreadCrumbProps> = ({}) => {
  return <div className=''><span className="text-muted-foreground">{'Home > Dashboard > Services'}</span></div>
}

export default BreadCrumb